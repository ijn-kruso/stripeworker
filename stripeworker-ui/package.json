{
  "name": "stripeworker-ui",
  "version": "0.0.1",
  "private": true,
  "description": "Stripe App UI Extension for Product CSV import/export",
  "scripts": {
    "start": "stripe apps start",
    "upload": "stripe apps upload",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stripe/ui-extension-sdk": "^10.0.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "typescript": "^5.0.0"
  }
}
